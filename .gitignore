# Claude Code local settings (user-specific)
.claude/settings.local.json

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environments
venv/
env/
ENV/

# Typst intermediate files (Typst doesn't create any, but include for safety)
# Note: Typst compiles directly to PDF without intermediate files

# OS files
.DS_Store
Thumbs.db

# Resume formats
*.doc
*.docx
*.pdf

# User resume files (ignore in root and .claude directories)
/*resume*.yaml
/*resume*.yml
.claude/**/*.yaml
.claude/**/*.yml

# Typst files (generated from YAML)
*.typ

# Resume version store (user data)
.resume_versions/

# Packaged skill distributions (build artifacts)
# dist/
*.skill
